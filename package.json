{
  "name": "aws-serverless-api",
  "version": "1.0.0",
  "description": "AWS Serverless API Example",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "serverless": "^3.0.0"
  }
}
